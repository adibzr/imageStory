// Interface for selecting and uploading images
// Handles image processing (resizing, format conversion)

const StoryUploader = () => {
  return <div>StoryUploader</div>;
};

export default StoryUploader;
